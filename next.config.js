/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If deploying to GitHub Pages with a subdirectory, uncomment:
  // basePath: '/tubb',
  // trailingSlash: true,
}

module.exports = nextConfig
